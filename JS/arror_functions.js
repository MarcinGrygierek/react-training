function foo(a, b) {
    return a + b;
}

foo(1, 2);

// zapis w postaci arrow function
const foo2 = (a, b) => a + b;

// działanie identyczne - inny zapis
const foo3 = (a, b) => {
    return a + b;
}